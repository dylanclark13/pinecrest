import {sqliteTable,text,integer,primaryKey,index} from 'drizzle-orm/sqlite-core';
export const players=sqliteTable('players',{userId:text('user_id').primaryKey(),xp:integer('xp').notNull().default(0),equipped:integer('equipped').notNull().default(0),tokens:integer('tokens').notNull().default(0),displayName:text('display_name').notNull().default('Golfer'),appearance:text('appearance').notNull().default('{}'),onboarded:integer('onboarded').notNull().default(0),clubLevels:text('club_levels').notNull().default('{}'),holes:integer('holes').notNull().default(0),rounds:integer('rounds').notNull().default(0)});
export const rounds=sqliteTable('rounds',{id:text('id').primaryKey(),userId:text('user_id').notNull(),course:integer('course').notNull(),mode:text('mode').notNull(),nextHole:integer('next_hole').notNull(),endHole:integer('end_hole').notNull(),status:text('status').notNull().default('active'),createdAt:integer('created_at').notNull()},t=>[index('idx_rounds_user_status').on(t.userId,t.status),index('idx_rounds_records').on(t.status,t.course,t.mode)]);
export const holes=sqliteTable('round_holes',{roundId:text('round_id').notNull(),hole:integer('hole').notNull(),strokes:integer('strokes').notNull(),xp:integer('xp').notNull(),tokens:integer('tokens').notNull().default(0),receipt:text('receipt').notNull()},t=>[primaryKey({columns:[t.roundId,t.hole]})]);
export const accounts=sqliteTable('accounts',{id:text('id').primaryKey(),email:text('email').notNull().unique(),playerId:text('player_id').notNull().unique(),passwordHash:text('password_hash').notNull(),salt:text('salt').notNull(),createdAt:integer('created_at').notNull()});
export const sessions=sqliteTable('sessions',{tokenHash:text('token_hash').primaryKey(),accountId:text('account_id').notNull(),expiresAt:integer('expires_at').notNull()},t=>[index('idx_sessions_account').on(t.accountId)]);
export const authLimits=sqliteTable('auth_limits',{key:text('key').primaryKey(),count:integer('count').notNull().default(0),expiresAt:integer('expires_at').notNull()});
